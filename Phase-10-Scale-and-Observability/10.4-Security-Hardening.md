---
phase: 10
phase_title: "Scale & Observability"
section: "10.4"
title: "Security Hardening"
status: done
tasks_total: 4
tasks_done: 4
week: "14–15"
color: success
tags:
  - phase-10
  - scale-and-observability
  - done
created: 2026-06-06
updated: 2026-06-09
---

# 10.4 — Security Hardening

> [!check] Phase 10 · Scale & Observability · Week 14–15
> **Goal:** Handle growth without breaking
> **Key Unlock:** Ready for growth

## Status

> [!success] ✅ Complete
> 4 of 4 tasks complete.

## Tasks

- [x] Add rate limiting to all API routes using Upstash Redis
- [x] Sanitize all user-supplied input (search queries, player names)
- [x] Verify Stripe webhook signature on all incoming events
- [x] Rotate DB credentials and add read-replica for analytics queries

## Implementation Notes

- `lib/rateLimit.ts` — sliding-window in-memory rate limiter (no external service needed); pre-defined limits: auth=10/15min, api=60/min, aiGen=5/min, search=30/min. Swap for `@upstash/ratelimit` in multi-instance production.
- `lib/sanitize.ts` — `stripHtml`, `sanitizeSearch`, `sanitizeName`, `sanitizeUrl`, `safeInt` helpers
- Applied rate limiting: `/api/auth/login` (10/15min per IP), `/api/auth/register` (10/15min per IP), `/api/analysis/[matchId]` (5/min per user), `/api/scouting/players` (30/min per user)
- Applied sanitization: `register` (name/email), `scouting/players` (search query), `org/create` (org name)
- Stripe webhook: `stripe.webhooks.constructEvent(body, sig, secret)` was already implemented in Phase 4 — rejects any request where HMAC verification fails (400)
- DB credential rotation: procedure documented — rotate via Neon dashboard → Connection details → Reset password; update `DATABASE_URL` in Vercel env vars; no code changes needed. Read-replica available via Neon branching.

## Navigation

- [[Phase-10-Scale-and-Observability/_index|← Back to Scale & Observability]]
- [[index|← Master Index]]
